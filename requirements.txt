Flask==3.1.2
requests==2.32.3
python-dotenv==1.0.0

# LangChain - Updated to a stable, compatible set in the 0.3.x series
langchain==0.2.22
langchain-core==0.2.22
langchain-community==0.2.22
langchain-text-splitters==0.2.2
langchain-huggingface==0.3.1 # Updated to a newer, compatible version
langchain-pinecone==0.3.0 # Updated to 0.3.0

# Pinecone client
pinecone-client==5.0.1

# Database
SQLAlchemy==2.0.44

# Utilities
tqdm==4.66.4
httpx==0.27.0
PyYAML==6.0.1

# Flask dependencies (NOTE: Werkzeug 3.1.3 is not yet available, using 3.0.3)
Werkzeug==3.0.3
Jinja2==3.1.4
itsdangerous==2.2.0
click==8.1.7
blinker==1.7.0 # NOTE: 1.9.0 is unavailable for Python 3.12, using 1.7.0
markupsafe>=2.1.1

# LangChain dependencies (These should be handled by the packages above, but good to keep as a fallback)
aiohttp>=3.8.3,<4.0.0
langsmith>=0.1.17,<0.2.0
numpy>=1.26.0,<2.0.0
tenacity>=8.1.0,<9.0.0
jsonpatch>=1.33,<2.0
packaging>=23.2,<25
typing-extensions>=4.7
dataclasses-json>=0.5.7,<0.7

# Requests dependencies
charset-normalizer>=2,<4
idna>=2.5,<4
urllib3>=1.21.1,<3
certifi>=2017.4.17
